# CI del harness

`harness-ci.yml` ejecuta las pruebas de Python en los pull requests de este repositorio y también de forma manual. No despliega a Databricks ni modifica el repositorio NaturaPet. El despliegue del bundle `dev` se hace manualmente con el perfil indicado en `docs/operacion.md`.
