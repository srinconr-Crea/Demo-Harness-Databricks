# Agentes

`developer/config.yaml` define Developer y expone Architect y Reviewer como subagentes de Omnigent. Los tres declaran el ejecutor Codex; el modelo GPT-6-Sol se fija al iniciar la sesión principal. Verifica el modelo efectivo de los subagentes en la traza antes de considerarlos comparables. El host de Omnigent debe ver la clonación de NaturaPet como directorio de trabajo.
