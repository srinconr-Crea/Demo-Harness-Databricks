resources:
  volumes:
    artifacts:
      catalog_name: ${resources.catalogs.harness_catalog.name}
      schema_name: ${resources.schemas.project_schema.name}
      name: artifacts
      volume_type: MANAGED
      comment: "MLflow artifacts for ${bundle.name}"
