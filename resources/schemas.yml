resources:
  schemas:
    project_schema:
      catalog_name: ${resources.catalogs.harness_catalog.name}
      name: ${var.schema}
      comment: "Resources, traces, and artifacts for ${bundle.name}"
