# Configuración de proyecto

Omnigent puede leer valores del proyecto desde `.omnigent/config.yaml`. Por ahora el executor y las políticas están en el YAML del agente para hacer explícita la comparación. Las credenciales, perfiles y configuración del servidor quedan fuera de este repositorio.
